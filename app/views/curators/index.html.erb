<h2>CURATORS</h2>

<% if current_curator %>
  <p>
    Welcome, <%= current_curator.username %>
  </p>
  <%= link_to "START A NEW COLLECTION", artworks_new_path %>
  <%= link_to "VIEW COLLECTIONS", curator_collections_path %>
  <%= link_to "LOG-OUT", log_out_path, method: :delete %>
<% else%>
  <%= link_to "LOG-IN", log_in_path %>
  <%= link_to "SIGN-UP", new_curator_path %>
<% end %>

<ul>
  <% @curators.each do |curator| %>
    <li>
      <a href='/curators/<%= curator.id %>'><%= curator.username %></a>
    </li>
  <% end %>
</ul>
